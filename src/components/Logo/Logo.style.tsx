import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
    logo:{
        width: '120px',
        height: '80px',
        margin: '16px 15px',
        
    }
})

export default useStyles;