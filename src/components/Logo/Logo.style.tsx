import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
    logo:{
        width: '120px',
        height: '80px',
        padding: '16px 10px'
    }
})

export default useStyles;